# API server URL the browser fetches against (Better Auth, /v1/*, etc.).
# Vite injects this at build time. Defaults work when portless runs on
# its standard :443 (auto-elevates with sudo on first run); if portless
# fell back to a custom port, override here with the matching `:port`.
VITE_SERVER_URL=https://api.batuda.localhost
